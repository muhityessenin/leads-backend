# 🚀 Быстрый старт Postman коллекции (3 минуты)

## Что вам нужно

- ✅ Postman (скачайте с [postman.com](https://www.postman.com/downloads/))
- ✅ Файл `postman_collection.json` из папки проекта
- ✅ Работающий сервер (бэкенд должен запущен на `http://localhost:3000`)

---

## Этап 1: Импорт коллекции (30 секунд)

### Шаг 1. Откройте Postman

### Шаг 2. Нажмите **Import**
- Кнопка в верхнем левом углу
- Или используйте меню: **File** → **Import**

### Шаг 3. Выберите вкладку **Upload Files**

### Шаг 4. Выберите файл `postman_collection.json`
- Находится в корне папки проекта `/leads-backend/`

### Шаг 5. Нажмите **Import**

✅ **Готово!** Коллекция появится в левой панели Postman.

---

## Этап 2: Проверка сервера (1 минута)

### Откройте папку "0. System Health"
1. **Нажмите** "Health Check"
2. **Нажмите кнопку Send** (синяя кнопка справа)
3. **Смотрите ответ** внизу:

```json
{
  "success": true,
  "message": "Server is running",
  "timestamp": "2025-12-09T14:40:43.699Z"
}
```

✅ Если вы видите `"success": true` — сервер работает!

---

## Этап 3: Полный цикл (1.5 минуты)

### 1. Регистрация
```
Папка: 1. Authentication
Запрос: Register
```

**Нажмите Send** → Вы получите JWT токен ✅

*(Токен автоматически сохранится в переменную)*

### 2. Получение профиля
```
Папка: 2. User Management
Запрос: Get My Profile
```

**Нажмите Send** → Вы увидите свой профиль ✅

*(Токен подставляется автоматически)*

### 3. Создание типа лида
```
Папка: 3. Lead Types
Запрос: Create Lead Type
```

**Нажмите Send** → Будет создан тип лида ✅

*(ID типа сохранится в переменную)*

### 4. Создание лида
```
Папка: 4. Leads
Запрос: Create Lead
```

**Нажмите Send** → Будет создан лид ✅

---

## Сопоставление переменных (автоматическое! 🤖)

Когда вы выполняете запросы, переменные заполняются **автоматически**:

| После запроса | Переменная | Что в ней |
|---|---|---|
| Register / Login | `{{token}}` | JWT токен для авторизации |
| Create Lead Type | `{{leadTypeId}}` | ID типа лида |
| Create Lead | `{{leadId}}` | ID лида |
| Create Order | `{{orderId}}` | ID заказа |

Все последующие запросы будут использовать эти значения автоматически! ✨

---

## Где посмотреть мою переменную?

**Вариант 1: В интерфейсе (быстро)**
```
Нажмите на запрос, где переменная используется
→ Смотрите строку URL
→ Переменная будет выделена
→ Наведите на неё мышку, чтобы увидеть значение
```

**Вариант 2: В Environments (полный список)**
1. **Откройте вкладку "Environments"** (левое меню)
2. **Выберите "Leads Backend API" или активное окружение**
3. **Смотрите все переменные и их значения**

---

## 💡 Полезные кнопки в Postman

| Кнопка | Что делает |
|--------|-----------|
| **Send** | Отправить запрос |
| **Save** | Сохранить изменения запроса |
| **Collections** | Показать/скрыть коллекции (левое меню) |
| **Environments** | Выбрать или создать окружение |
| **History** | Показать историю недавних запросов |
| **Console** | Открыть консоль для отладки |

---

## ❌ Частые ошибки

### Ошибка: "Cannot GET /api/auth/register"

**Решение:**
1. Убедитесь, что `{{base_url}}` = `http://localhost:3000`
2. Проверьте, запущен ли бэкенд (`docker compose up -d`)
3. Попробуйте Health Check

### Ошибка: "401 Unauthorized"

**Решение:**
1. Выполните Register или Login
2. Убедитесь, что `{{token}}` не пуст
3. Смотрите переменные в Environments

### Ошибка: "leadTypeId is undefined"

**Решение:**
1. Выполните "Create Lead Type" перед "Create Lead"
2. Убедитесь, что ответ был успешный (код 201)
3. Проверьте переменные в Environments

---

## 📖 Больше информации?

Полная подробная документация находится в файле **`POSTMAN_GUIDE.md`**

Откройте в текстовом редакторе или на GitHub для полного справочника.

---

## 🎯 Что дальше?

После импорта вы можете:

✅ **Тестировать API** — выполняйте запросы и смотрите ответы  
✅ **Экспортировать документацию** — Postman автоматически создаст HTML-документацию  
✅ **Делиться коллекцией** — поделитесь с командой через Postman Cloud  
✅ **Автоматизировать** — используйте Collection Runner для регулярного тестирования  

---

**Готово!** Вы готовы тестировать API 🎉
