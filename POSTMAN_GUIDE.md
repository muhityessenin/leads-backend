# Postman Collection - Полная инструкция по применению

## 📋 Содержание
1. [Описание коллекции](#описание)
2. [Установка](#установка)
3. [Настройка окружения](#настройка)
4. [Использование](#использование)
5. [Примеры запросов](#примеры)
6. [Советы и трюки](#советы)

---

## <a name="описание"></a>📖 Описание коллекции

**Leads Backend API - Complete Documentation** — полная Postman-коллекция для тестирования и документирования всех endpoint'ов бэкенда.

### Включает следующие разделы:

| Раздел | Описание | Endpoints |
|--------|---------|-----------|
| **0. System Health** | Проверка состояния сервера | 1 endpoint |
| **1. Authentication** | Регистрация и вход | 2 endpoints |
| **2. User Management** | Управление профилем | 1 endpoint |
| **3. Lead Types** | Управление типами лидов | 3 endpoints |
| **4. Leads** | Управление лидами | 2 endpoints |
| **5. Orders** | Управление заказами | 1 endpoint |
| **6. Payments** | Управление платежами | 1 endpoint |
| **7. Consent** | Управление согласиями | 1 endpoint |

---

## <a name="установка"></a>🚀 Установка

### Вариант 1: Прямой импорт из файла (Рекомендуется)

1. **Откройте Postman** (десктопное приложение)
2. **Нажмите "Import"** (верхнее левое меню)
3. **Выберите вкладку "Upload Files"**
4. **Выберите файл** `postman_collection.json` из папки проекта
5. **Нажмите "Import"**

### Вариант 2: Через URL (если файл на сервере)

```
Файл находится по пути: /leads-backend/postman_collection.json
```

### Вариант 3: Ручное копирование (если импорт не работает)

1. Откройте `postman_collection.json` текстовым редактором
2. Скопируйте всё содержимое
3. В Postman: **New** → **Collection** → вставьте JSON
4. Нажмите **Create**

---

## <a name="настройка"></a>⚙️ Настройка окружения

### Автоматическая настройка переменных

Коллекция уже содержит переменные, которые заполняются автоматически при выполнении запросов:

| Переменная | Описание | Используется |
|-----------|---------|--------------|
| `{{base_url}}` | URL сервера | Все requests |
| `{{token}}` | JWT токен | Авторизованные requests |
| `{{leadTypeId}}` | ID типа лида | Requests с лидами |
| `{{leadId}}` | ID лида | Requests с заказами/платежами |
| `{{orderId}}` | ID заказа | Requests с платежами |

### Ручная настройка (если нужно изменить URL)

1. **Откройте коллекцию** в Postman
2. **Нажмите на название коллекции** → **Variables**
3. **Измените значение `base_url`**:
   - Для локального окружения: `http://localhost:3000`
   - Для production: `https://api.yourdomain.com`
4. **Нажмите "Save"**

---

## <a name="использование"></a>📡 Использование коллекции

### Рекомендуемый порядок использования:

#### Шаг 1️⃣: Проверка сервера

```
GET /api/health
```

- **Что делает:** Проверяет, работает ли сервер
- **Авторизация:** Не требуется
- **Ожидаемый результат:** `{"success": true, "message": "Server is running"}`

#### Шаг 2️⃣: Регистрация пользователя

```
POST /api/auth/register
```

**Тело запроса:**
```json
{
  "email": "myuser@example.com",
  "password": "MySecurePassword123"
}
```

**Что произойдёт:**
- Пользователь будет создан в БД
- Вы получите JWT токен
- **Токен автоматически сохранится в переменную `{{token}}`** ✅

#### Шаг 3️⃣: Получение профиля

```
GET /api/users/profile
```

**Авторизация автоматически:** используется `{{token}}` из шага 2️⃣

#### Шаг 4️⃣: Создание типа лида

```
POST /api/lead-types
```

**Тело:**
```json
{
  "title": "My Lead Type",
  "description": "Description here",
  "basePrice": 9.99
}
```

**Результат:** ID типа сохранится в `{{leadTypeId}}`

#### Шаг 5️⃣-7️⃣: Остальные операции

Используйте созданные ID в последующих запросах.

---

## <a name="примеры"></a>💡 Примеры использования

### Пример 1: Полный цикл (Регистрация → Профиль)

```
1. Health Check (GET /api/health)
   ✓ Проверяем, что сервер работает

2. Register (POST /api/auth/register)
   Input: email, password
   Output: token ✅ (сохранится автоматически)

3. Get Profile (GET /api/users/profile)
   Uses: {{token}} (автоматически подставится)
   Output: User data
```

### Пример 2: Работа с лидами

```
1. Create Lead Type (POST /api/lead-types)
   Output: leadTypeId ✅

2. Create Lead (POST /api/leads)
   Input: leadTypeId (из предыдущего шага)
   Output: leadId ✅

3. Create Order (POST /api/orders)
   Input: leadId (из предыдущего шага)
   Output: orderId ✅

4. Create Payment (POST /api/payments)
   Input: orderId (из предыдущего шага)
```

### Пример 3: Использование переменных в URL

В URL запроса используйте двойные фигурные скобки:

```
{{base_url}}/api/lead-types/{{leadTypeId}}
```

Postman автоматически подставит значения переменных.

---

## <a name="советы"></a>💎 Советы и трюки

### 1️⃣ Просмотр описания endpoint'а

1. Нажмите на запрос в коллекции
2. Нажмите кнопку **"Description"** (справа)
3. Прочитайте полное описание, параметры и возможные ошибки

### 2️⃣ Проверка сохранённых переменных

1. **Откройте вкладку "Environments"** (левое меню)
2. **Выберите нужное окружение**
3. **看отображённые переменные и их значения**

### 3️⃣ Отправка нескольких запросов подряд

Используйте **Collection Runner**:

1. Нажмите кнопку **▶️ Run** (справа сверху)
2. Выберите коллекцию
3. Установите опции:
   - **Delay between requests**: 1000ms (1 секунда)
   - **Persist responses**: ✅ (для сохранения ответов)
4. Нажмите **▶️ Run**

### 4️⃣ Просмотр истории запросов

1. Нажмите на вкладку **"History"** (левое меню)
2. Все ваши недавние запросы там будут

### 5️⃣ Экспорт результатов

После запуска Collection Runner:

1. **Нажмите "Export Results"**
2. **Выберите формат** (JSON, CSV, HTML)
3. **Сохраните файл**

### 6️⃣ Отладка запросов

Включите **Postman Console** (Ctrl/Cmd + Alt + C):

```
✓ Увидите all HTTP requests
✓ Response headers
✓ Response body
✓ Network timings
✓ Переменные окружения
```

### 7️⃣ Тестирование автоматически

Каждый запрос уже содержит **тесты**. Смотрите результаты в **"Tests"** вкладке:

```
✅ Test passed
❌ Test failed (красный)
```

---

## 📊 Структура JSON файла

```json
{
  "info": {
    "_postman_id": "leads-backend-api-v1",
    "name": "Коллекция",
    "description": "Полное описание"
  },
  "item": [
    {
      "name": "Раздел (папка)",
      "item": [
        {
          "name": "Запрос",
          "request": {
            "method": "GET/POST/PUT/DELETE",
            "header": [],
            "body": {},
            "url": {}
          }
        }
      ]
    }
  ],
  "variable": [
    {
      "key": "имя переменной",
      "value": "значение"
    }
  ]
}
```

---

## 🔍 Быстрая справка по HTTP методам

| Метод | Назначение | Примеры |
|-------|-----------|---------|
| **GET** | Получить данные | Профиль, список лидов |
| **POST** | Создать данные | Регистрация, создание лида |
| **PUT** | Обновить данные | Изменение лида (если есть) |
| **DELETE** | Удалить данные | Удаление лида (если есть) |

---

## 🛠️ Решение проблем

### ❌ "401 Unauthorized"

**Причина:** Токен не установлен или истёк

**Решение:**
1. Выполните запрос **Register** или **Login**
2. Убедитесь, что токен сохранился в переменную `{{token}}`
3. Повторите запрос

### ❌ "Cannot GET /api/..."

**Причина:** Неверный URL или сервер не запущен

**Решение:**
1. Проверьте, работает ли сервер: `GET /api/health`
2. Убедитесь, что `{{base_url}}` правильный
3. Проверьте URL endpoint'а

### ❌ "leadTypeId is undefined"

**Причина:** Переменная не установлена

**Решение:**
1. Выполните **Create Lead Type** перед **Create Lead**
2. Убедитесь, что запрос успешен (код 201)
3. Проверьте переменные в консоли

### ❌ Переменные не подставляются

**Причина:** Неверный синтаксис

**Решение:**
- ✅ Правильно: `{{base_url}}`
- ❌ Неправильно: `{base_url}` или `[base_url]`

---

## 📱 Использование на мобильных устройствах

Postman доступен для iOS и Android:

1. **Скачайте приложение** из App Store или Google Play
2. **Импортируйте коллекцию** через экспорт с десктопа
3. **Синхронизируйте** через Postman Cloud (требуется аккаунт)

---

## 📚 Дополнительные ресурсы

- [Официальная документация Postman](https://learning.postman.com/)
- [Postman Learning Center](https://learning.postman.com/docs/getting-started/overview/)
- [API Documentation Best Practices](https://learning.postman.com/docs/publishing-your-api/documenting-your-api/)

---

## ✅ Контрольный список перед использованием

- [ ] Postman установлен и открыт
- [ ] Файл `postman_collection.json` скачан
- [ ] Коллекция успешно импортирована
- [ ] Переменная `{{base_url}}` указывает на ваш сервер
- [ ] Сервер работает (проверьте `/api/health`)
- [ ] Вы готовы начать тестирование! 🚀

---

**Последнее обновление:** 9 декабря 2025 г.  
**Версия:** 1.0.0  
**Поддержка:** Обратитесь к документации API или в техподдержку
