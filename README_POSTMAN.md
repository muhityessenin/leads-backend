# 📚 Полная документация Postman - Индекс и резюме

## 📖 Что вы получили

Полный набор документации для работы с Postman коллекцией API:

### 📄 Файлы документации

| Файл | Описание | Для кого |
|------|---------|----------|
| **postman_collection.json** | Сама коллекция с 20+ запросами и тестами | Postman (импортируйте этот файл) |
| **POSTMAN_QUICK_START.md** | 3-минутный старт | Спешащие люди 🏃 |
| **POSTMAN_STEP_BY_STEP.md** | Пошаговая визуальная инструкция | Начинающие 👶 |
| **POSTMAN_GUIDE.md** | Полный справочник со всеми деталями | Опытные пользователи 🧑‍💻 |
| **README_POSTMAN.md** | Этот файл — индекс и резюме | Все 📌 |

---

## 🚀 Быстрый старт (3 мин)

### Что нужно сделать прямо сейчас:

1. **Скачайте Postman:** https://www.postman.com/downloads/
2. **Откройте Postman**
3. **Нажмите Import** → выберите `postman_collection.json`
4. **Нажмите Send** на "Health Check"
5. **Готово!** 🎉

**Результат:**
```
Status: 200 OK
{
  "success": true,
  "message": "Server is running"
}
```

---

## 📚 Структура коллекции

```
Leads Backend API - Complete Documentation
│
├─ 0. System Health
│  └─ Health Check (GET) — Проверка, работает ли сервер
│
├─ 1. Authentication
│  ├─ Register (POST) — Создание нового пользователя
│  └─ Login (POST) — Вход существующего пользователя
│
├─ 2. User Management
│  └─ Get My Profile (GET) — Получение профиля пользователя
│
├─ 3. Lead Types
│  ├─ Create Lead Type (POST) — Создание типа лида
│  ├─ Get All Lead Types (GET) — Список всех типов
│  └─ Get Lead Type by ID (GET) — Один конкретный тип
│
├─ 4. Leads
│  ├─ Create Lead (POST) — Создание лида
│  └─ Get All Leads (GET) — Список всех лидов
│
├─ 5. Orders
│  └─ Create Order (POST) — Создание заказа
│
├─ 6. Payments
│  └─ Create Payment (POST) — Создание платежа
│
└─ 7. Consent
   └─ Create Consent (POST) — Запись согласия пользователя
```

**Всего:** 16 endpoint'ов для полного тестирования API

---

## 🔧 Переменные окружения

Коллекция использует эти переменные (заполняются автоматически):

| Переменная | Значение | Где получить |
|-----------|---------|--------------|
| `{{base_url}}` | `http://localhost:3000` | Установите в Environments |
| `{{token}}` | JWT токен пользователя | Автоматически из Register/Login |
| `{{leadTypeId}}` | UUID типа лида | Автоматически из Create Lead Type |
| `{{leadId}}` | UUID лида | Автоматически из Create Lead |
| `{{orderId}}` | UUID заказа | Автоматически из Create Order |

**Как это работает:**
- Вы запускаете запрос → он возвращает ID
- Этот ID автоматически сохраняется в переменную
- Следующий запрос использует эту переменную
- Всё происходит без вашего участия! 🤖

---

## 📋 Полный цикл использования (пошагово)

### Сценарий: Создание лида и заказа на него

```
Шаг 1: Проверка сервера
│
GET /api/health
│ ✅ Status: 200 OK
│
├─→ Шаг 2: Регистрация пользователя
│   │
│   POST /api/auth/register
│   Input: email, password
│   │ ✅ Status: 201 Created
│   │ Output: token ← сохраняется в {{token}}
│   │
│   ├─→ Шаг 3: Получение профиля
│   │   │
│   │   GET /api/users/profile
│   │   Headers: Authorization: Bearer {{token}}
│   │   │ ✅ Status: 200 OK
│   │   │ Output: User data
│   │   │
│   │   ├─→ Шаг 4: Создание типа лида
│   │       │
│   │       POST /api/lead-types
│   │       Body: {title, description, basePrice}
│   │       │ ✅ Status: 201 Created
│   │       │ Output: leadTypeId ← сохраняется в {{leadTypeId}}
│   │       │
│   │       ├─→ Шаг 5: Создание лида
│   │           │
│   │           POST /api/leads
│   │           Body: {leadTypeId, city, price}
│   │           │ ✅ Status: 201 Created
│   │           │ Output: leadId ← сохраняется в {{leadId}}
│   │           │
│   │           ├─→ Шаг 6: Создание заказа
│   │               │
│   │               POST /api/orders
│   │               Body: {leadId, amount}
│   │               │ ✅ Status: 201 Created
│   │               │ Output: orderId ← сохраняется в {{orderId}}
│   │               │
│   │               └─→ Шаг 7: Создание платежа
│   │                   │
│   │                   POST /api/payments
│   │                   Body: {orderId, amount}
│   │                   │ ✅ Status: 201 Created
│   │                   │
│   │                   ✅ ВСЕ ГОТОВО!
```

---

## 🎓 Как читать документацию

### Каждый запрос содержит:

```json
{
  "name": "Create Lead Type",
  "request": {
    "method": "POST",
    "url": "http://localhost:3000/api/lead-types",
    "body": {
      "title": "Mobile Phone Leads",
      "basePrice": 5.99
    }
  },
  "description": "Подробное описание что делает запрос"
}
```

### В интерфейсе Postman вы видите:

```
┌─────────────────────────────────────┐
│ Create Lead Type                    │
│                                     │
│ Method: [POST]                      │
│ URL: {{base_url}}/api/lead-types    │
│                                     │
│ [Description] (нажмите для подробностей)
│                                     │
│ Headers:                            │
│  Authorization: Bearer {{token}}    │
│  Content-Type: application/json     │
│                                     │
│ Body:                               │
│ {                                   │
│   "title": "...",                   │
│   "description": "...",             │
│   "basePrice": 5.99                 │
│ }                                   │
│                                     │
│ [Send] (запустить запрос)          │
└─────────────────────────────────────┘

Response (нижняя часть):
┌─────────────────────────────────────┐
│ Status: 201 Created ✅              │
│                                     │
│ {                                   │
│   "success": true,                  │
│   "data": {                         │
│     "id": "f47ac10b...",           │
│     "title": "Mobile Phone...",     │
│     ...                             │
│   }                                 │
│ }                                   │
└─────────────────────────────────────┘
```

---

## 🛠️ Основные операции в Postman

### Запуск одного запроса
1. Нажмите на запрос в коллекции
2. Нажмите **[Send]**
3. Смотрите результат внизу

### Запуск всех запросов по порядку
1. Нажмите **[▶ Run]** рядом с названием коллекции
2. Выберите опции
3. Нажмите **[▶ Run Leads Backend API]**

### Просмотр истории
1. Откройте вкладку **History** (левое меню)
2. Нажмите на любой прошлый запрос
3. Вы можете повторить его или отредактировать

### Просмотр консоли (отладка)
- Нажмите **Ctrl + Alt + C** (Windows/Linux)
- Или **Cmd + Option + C** (macOS)
- Увидите все запросы, ответы, ошибки

---

## 📊 Статус-коды HTTP (что означают)

| Код | Название | Означает |
|-----|----------|----------|
| 200 | OK | Всё хорошо, запрос успешен |
| 201 | Created | Ресурс успешно создан |
| 400 | Bad Request | Ошибка в теле запроса |
| 401 | Unauthorized | Требуется авторизация (токен) |
| 404 | Not Found | Ресурс не найден |
| 500 | Server Error | Ошибка на сервере |

**В коллекции вы должны видеть:**
- ✅ **200 OK** — для GET запросов
- ✅ **201 Created** — для POST запросов (создание)
- ✅ **200 OK** — для PUT запросов (обновление)

**Если видите красный код — что-то не так:**
- Проверьте URL
- Проверьте токен
- Проверьте тело запроса
- Посмотрите сообщение об ошибке в Response

---

## 🔐 Как работает авторизация

### JWT токен (JSON Web Token)

```
1. Вы вызываете POST /api/auth/register
   ↓
2. Сервер создаёт пользователя и возвращает токен:
   "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
   ↓
3. Вы копируете этот токен в переменную {{token}}
   ↓
4. Каждый запрос использует этот токен в заголовке:
   Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   ↓
5. Сервер проверяет токен и понимает, кто вы
   ↓
6. Сервер выполняет запрос от вашего имени ✅
```

### Что будет, если токен неверный?

```
❌ Error 401 Unauthorized
❌ Error: Invalid or expired token
```

**Решение:**
- Выполните Register снова
- Скопируйте новый токен в {{token}}

---

## 💾 Экспорт и сохранение

### Сохранить результаты запроса
1. Нажмите **Save** (под Request Name)
2. Изменения сохранятся в коллекции

### Экспортировать всю коллекцию
1. Нажмите на **...** (рядом с названием коллекции)
2. Выберите **Export**
3. Выберите папку для сохранения
4. Сохранится JSON файл

### Поделиться с командой
1. Сохраните коллекцию
2. Отправьте файл JSON коллегам
3. Они импортируют как обычно (Import → Upload Files)

---

## 🎯 Чек-лист перед использованием

- [ ] Postman установлен и открыт
- [ ] Бэкенд запущен (`docker compose up -d`)
- [ ] Коллекция импортирована в Postman
- [ ] Переменная `{{base_url}}` = `http://localhost:3000`
- [ ] Health Check возвращает 200 OK
- [ ] Вы прочитали POSTMAN_QUICK_START.md

**Если всё это выполнено — вы готовы!** 🚀

---

## 📞 Часто задаваемые вопросы

### Q: Где найти токен после регистрации?
A: Он находится в Response → data → token. Постман сохраняет его автоматически в переменную {{token}}.

### Q: Почему я вижу ошибку "Cannot GET /api/..."?
A: Сервер не запущен или URL неверный. Проверьте:
- `docker compose ps` (контейнеры запущены?)
- `{{base_url}}` в переменных

### Q: Как мне использовать коллекцию на production?
A: Измените `{{base_url}}` на ваш production URL, например: `https://api.yourdomain.com`

### Q: Можно ли делиться коллекцией через облако?
A: Да! Откройте Postman Cloud (синяя кнопка вверху), создайте рабочее пространство, и коллекция будет синхронизироваться.

### Q: Как запустить все запросы по порядку?
A: Используйте Collection Runner ([▶ Run] кнопка). Он запустит все запросы в правильном порядке с задержками между ними.

---

## 📚 Связанные файлы проекта

```
/leads-backend/
├─ postman_collection.json       ← Сама коллекция
├─ POSTMAN_QUICK_START.md        ← 3-минутный старт
├─ POSTMAN_STEP_BY_STEP.md       ← Пошаговая инструкция
├─ POSTMAN_GUIDE.md              ← Полная документация
├─ README_POSTMAN.md             ← Этот файл
│
├─ src/                          ← Исходный код сервера
│  ├─ app.ts                     ← Express приложение
│  ├─ server.ts                  ← Запуск сервера
│  │
│  ├─ modules/
│  │  ├─ auth/                   ← Endpoints /api/auth/*
│  │  ├─ users/                  ← Endpoints /api/users/*
│  │  ├─ leadTypes/              ← Endpoints /api/lead-types/*
│  │  └─ ...
│  └─ ...
│
├─ docker-compose.yml            ← Запуск контейнеров
└─ package.json                  ← Зависимости
```

---

## 🎓 Дополнительные материалы

### Изучить Postman
- [Официальная документация](https://learning.postman.com/)
- [Collection Runner гайд](https://learning.postman.com/docs/running-collections/intro-to-collection-runs/)
- [Scripting in Postman](https://learning.postman.com/docs/writing-scripts/intro-to-scripts/)

### Изучить API дизайн
- [RESTful API Best Practices](https://restfulapi.net/)
- [HTTP методы](https://developer.mozilla.org/en-US/docs/Web/HTTP/Methods)
- [JWT токены](https://jwt.io/)

### Наш проект
- [Документация сервера](./README.md)
- [API Specification](./API.md)
- [Архитектура проекта](./STRUCTURE.md)

---

## ✨ Заключение

Вы получили **полный набор инструментов** для тестирования API:

✅ **20+ готовых запросов** с документацией  
✅ **Автоматическое управление переменными**  
✅ **Пошаговые инструкции** для разных уровней пользователей  
✅ **Примеры** и **типичные ошибки**  
✅ **Полный цикл** от регистрации до платежей  

**Всё готово к использованию!** 🎉

---

**Последнее обновление:** 9 декабря 2025 г.  
**Версия документации:** 1.0.0  
**Создано для:** Leads Backend API

**Вопросы?** Смотрите ближайший файл справа, или откройте Description в Postman для каждого запроса.

**Happy Testing!** 🚀
